// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.27.0
// source: education.sql

package dbgen

import (
	"context"
	"time"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const educationChecklistItems = `-- name: EducationChecklistItems :many
SELECT i.checklist_code, i.ordinal, i.observation_code, i.text_en, i.text_bn, i.is_critical
  FROM core.education_checklist_item i
  JOIN core.education_checklist c ON c.code = i.checklist_code
 WHERE i.retired_at IS NULL AND c.retired_at IS NULL
 ORDER BY i.checklist_code, i.ordinal
`

type EducationChecklistItemsRow struct {
	ChecklistCode   string
	Ordinal         int32
	ObservationCode string
	TextEn          string
	TextBn          string
	IsCritical      bool
}

func (q *Queries) EducationChecklistItems(ctx context.Context) ([]EducationChecklistItemsRow, error) {
	rows, err := q.db.Query(ctx, educationChecklistItems)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []EducationChecklistItemsRow{}
	for rows.Next() {
		var i EducationChecklistItemsRow
		if err := rows.Scan(
			&i.ChecklistCode,
			&i.Ordinal,
			&i.ObservationCode,
			&i.TextEn,
			&i.TextBn,
			&i.IsCritical,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const educationChecklists = `-- name: EducationChecklists :many
SELECT c.code, c.device_type, c.title_en, c.title_bn
  FROM core.education_checklist c
  JOIN core.education_device_type d ON d.code = c.device_type
 WHERE c.retired_at IS NULL AND d.retired_at IS NULL
 ORDER BY d.ordering
`

type EducationChecklistsRow struct {
	Code       string
	DeviceType string
	TitleEn    string
	TitleBn    string
}

func (q *Queries) EducationChecklists(ctx context.Context) ([]EducationChecklistsRow, error) {
	rows, err := q.db.Query(ctx, educationChecklists)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []EducationChecklistsRow{}
	for rows.Next() {
		var i EducationChecklistsRow
		if err := rows.Scan(
			&i.Code,
			&i.DeviceType,
			&i.TitleEn,
			&i.TitleBn,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const educationChecklistsForProducts = `-- name: EducationChecklistsForProducts :many
SELECT DISTINCT r.checklist_code, c.device_type, p.id AS product_id
  FROM core.medication_product p
  JOIN core.generic g ON g.id = p.generic_id
  JOIN core.education_device_rule r
    ON r.retired_at IS NULL
   AND (r.match_generic_id IS NULL OR r.match_generic_id = p.generic_id)
   AND (r.match_class_code IS NULL OR r.match_class_code = g.class_code)
   AND (r.match_dispense_unit IS NULL OR r.match_dispense_unit = p.dispense_unit)
   AND (r.match_form_code IS NULL OR r.match_form_code = p.form_code)
  JOIN core.education_checklist c ON c.code = r.checklist_code AND c.retired_at IS NULL
 WHERE p.id = ANY ($1::uuid[])
   AND p.facility_id = $2::uuid
   AND (r.match_generic_id IS NOT NULL
        OR NOT EXISTS (
          SELECT 1 FROM core.education_device_rule specific
           WHERE specific.retired_at IS NULL
             AND specific.match_generic_id = p.generic_id
             AND (specific.match_dispense_unit IS NULL
                  OR specific.match_dispense_unit = p.dispense_unit)
             AND (specific.match_form_code IS NULL
                  OR specific.match_form_code = p.form_code)))
`

type EducationChecklistsForProductsParams struct {
	ProductIds []uuid.UUID
	FacilityID uuid.UUID
}

type EducationChecklistsForProductsRow struct {
	ChecklistCode string
	DeviceType    string
	ProductID     uuid.UUID
}

// CP92's first acceptance criterion, as one query.
//
// Which checklist a prescribed line brings up is decided by what the formulary says the product
// *is* — its class, what it is dispensed in, what form it takes — and never by its name. A match
// on a trade name silently stops matching the day a manufacturer renames a brand, and a
// checklist that silently fails to appear is indistinguishable, on every screen, from a patient
// who is not on a device.
//
// A NULL column on a rule means "any". `match_class_code` alone would put the vial checklist in
// front of a pen patient, because insulin in a pen and insulin in a vial are the same class;
// `match_dispense_unit` alone would put the insulin checklist in front of anybody prescribed
// anything in a pen. Both together is why the rule table has four nullable columns rather than
// one.
//
// # The molecule outranks the class, and that is this query's own rule (spec §6.4)
//
// Semaglutide and dulaglutide are weekly; liraglutide is once daily; all three are one class. A
// rule naming the molecule therefore has to beat a rule naming only the class — otherwise a
// Victoza patient collects both checklists and is asked, on one screen, both which day of the
// week and what time of day they inject.
//
// The precedence is here rather than as a `priority` column because it is a property of
// *matching* and not of any one row: "the most specific rule that matched this product wins" is
// a statement about the set, and a number on each row would let two people disagree about it by
// editing one of them. The NOT EXISTS below is that sentence.
func (q *Queries) EducationChecklistsForProducts(ctx context.Context, arg EducationChecklistsForProductsParams) ([]EducationChecklistsForProductsRow, error) {
	rows, err := q.db.Query(ctx, educationChecklistsForProducts, arg.ProductIds, arg.FacilityID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []EducationChecklistsForProductsRow{}
	for rows.Next() {
		var i EducationChecklistsForProductsRow
		if err := rows.Scan(&i.ChecklistCode, &i.DeviceType, &i.ProductID); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const educationComplianceForVisit = `-- name: EducationComplianceForVisit :many
SELECT o.code, o.value_num, o.value_code, o.effective_at, o.recorded_at,
       o.recorded_by, o.recorded_role, o.station_code, o.source
  FROM read.observation o
 WHERE o.patient_id = $1::uuid
   AND o.facility_id = $2::uuid
   AND o.visit_id = $3::uuid
   AND o.status = 'ACTIVE'
   AND o.code IN ('MEDICATION_MISSED_DOSES_7D', 'MEDICATION_MISS_REASON')
 ORDER BY o.code, o.value_code
`

type EducationComplianceForVisitParams struct {
	PatientID  uuid.UUID
	FacilityID uuid.UUID
	VisitID    uuid.UUID
}

type EducationComplianceForVisitRow struct {
	Code         string
	ValueNum     pgtype.Numeric
	ValueCode    string
	EffectiveAt  time.Time
	RecordedAt   time.Time
	RecordedBy   uuid.UUID
	RecordedRole string
	StationCode  string
	Source       string
}

// What the patient said about missed doses at this visit, with who asked.
//
// Both codes in one query and the caller separates them, for the reason the improvement answer
// is read the same way: the count and the reasons are two different observations and either can
// be present without the other. A patient who said "three" and would not say why has answered
// the question, and a screen that required both to draw either would show nothing.
func (q *Queries) EducationComplianceForVisit(ctx context.Context, arg EducationComplianceForVisitParams) ([]EducationComplianceForVisitRow, error) {
	rows, err := q.db.Query(ctx, educationComplianceForVisit, arg.PatientID, arg.FacilityID, arg.VisitID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []EducationComplianceForVisitRow{}
	for rows.Next() {
		var i EducationComplianceForVisitRow
		if err := rows.Scan(
			&i.Code,
			&i.ValueNum,
			&i.ValueCode,
			&i.EffectiveAt,
			&i.RecordedAt,
			&i.RecordedBy,
			&i.RecordedRole,
			&i.StationCode,
			&i.Source,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const educationDeviceTypes = `-- name: EducationDeviceTypes :many

SELECT code, name_en, name_bn, ordering
  FROM core.education_device_type
 WHERE retired_at IS NULL
 ORDER BY ordering
`

type EducationDeviceTypesRow struct {
	Code     string
	NameEn   string
	NameBn   string
	Ordering int32
}

// The education station's reads (CP88, CP92).
//
// Every statement here is a SELECT. The station writes through `clinical.Service`, which writes
// through the ledger and the observation projection; `dthcms_app` holds no INSERT on
// `read.observation` and none on any table in this file.
func (q *Queries) EducationDeviceTypes(ctx context.Context) ([]EducationDeviceTypesRow, error) {
	rows, err := q.db.Query(ctx, educationDeviceTypes)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []EducationDeviceTypesRow{}
	for rows.Next() {
		var i EducationDeviceTypesRow
		if err := rows.Scan(
			&i.Code,
			&i.NameEn,
			&i.NameBn,
			&i.Ordering,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const educationEarlierVisitExists = `-- name: EducationEarlierVisitExists :one
SELECT EXISTS (
  SELECT 1 FROM core.visit v
   WHERE v.patient_id = $1::uuid
     AND v.facility_id = $2::uuid
     AND v.id <> $3::uuid
     AND v.opened_at < (SELECT opened_at FROM core.visit WHERE id = $3::uuid)
) AS had_one
`

type EducationEarlierVisitExistsParams struct {
	PatientID  uuid.UUID
	FacilityID uuid.UUID
	VisitID    uuid.UUID
}

// Whether this patient has been here before, which is what decides whether §2's question has a
// comparison point at all.
//
// Counted against closed and open visits other than this one rather than against observations:
// a patient who attended and had nothing recorded still has a last visit to compare with, and a
// question anchored to "your last visit" is anchored to the visit, not to its contents.
func (q *Queries) EducationEarlierVisitExists(ctx context.Context, arg EducationEarlierVisitExistsParams) (bool, error) {
	row := q.db.QueryRow(ctx, educationEarlierVisitExists, arg.PatientID, arg.FacilityID, arg.VisitID)
	var had_one bool
	err := row.Scan(&had_one)
	return had_one, err
}

const educationItemStates = `-- name: EducationItemStates :many
SELECT a.value_code, a.display_en, a.display_bn, a.ordering
  FROM core.observation_answer a
 WHERE a.code = (SELECT i.observation_code
                   FROM core.education_checklist_item i
                   JOIN core.education_checklist c ON c.code = i.checklist_code
                  WHERE i.retired_at IS NULL AND c.retired_at IS NULL
                  ORDER BY i.checklist_code, i.ordinal
                  LIMIT 1)
   AND a.retired_at IS NULL
 ORDER BY a.ordering
`

type EducationItemStatesRow struct {
	ValueCode string
	DisplayEn string
	DisplayBn string
	Ordering  int32
}

// The three states, read off the observation vocabulary rather than off a list in Go.
//
// One item's answers stand for all of them: the invariant in migration 00070 asserts that every
// live checklist item is answerable in exactly these three, so reading one code's vocabulary and
// reading all thirty-eight would give the same answer — and reading one keeps the reference
// payload from carrying a hundred and fourteen identical rows.
func (q *Queries) EducationItemStates(ctx context.Context) ([]EducationItemStatesRow, error) {
	rows, err := q.db.Query(ctx, educationItemStates)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []EducationItemStatesRow{}
	for rows.Next() {
		var i EducationItemStatesRow
		if err := rows.Scan(
			&i.ValueCode,
			&i.DisplayEn,
			&i.DisplayBn,
			&i.Ordering,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const educationLatestCompetency = `-- name: EducationLatestCompetency :many
SELECT DISTINCT ON (o.code)
       o.code, o.value_code, o.effective_at, o.recorded_at, o.visit_id,
       o.recorded_by, o.recorded_role, o.station_code, o.source,
       i.checklist_code, i.ordinal, i.text_en, i.text_bn, i.is_critical
  FROM read.observation o
  JOIN core.education_checklist_item i ON i.observation_code = o.code
 WHERE o.patient_id = $1::uuid
   AND o.facility_id = $2::uuid
   AND o.status = 'ACTIVE'
 ORDER BY o.code, o.effective_at DESC, o.global_seq DESC
`

type EducationLatestCompetencyParams struct {
	PatientID  uuid.UUID
	FacilityID uuid.UUID
}

type EducationLatestCompetencyRow struct {
	Code          string
	ValueCode     string
	EffectiveAt   time.Time
	RecordedAt    time.Time
	VisitID       uuid.NullUUID
	RecordedBy    uuid.UUID
	RecordedRole  string
	StationCode   string
	Source        string
	ChecklistCode string
	Ordinal       int32
	TextEn        string
	TextBn        string
	IsCritical    bool
}

// What this patient was last seen able to do, one row per item.
//
// DISTINCT ON rather than a window function because the answer wanted is "the most recent state
// of each item", and a patient seen four times has four rows per item of which exactly one is
// the current fact. Superseded and corrected rows are excluded by `status = 'ACTIVE'`, which is
// the observation model's own definition of "this is the value".
//
// `global_seq` breaks the tie after `effective_at`, and the tie is real rather than theoretical:
// two assessments recorded in the same second — a correction typed immediately after the
// original, an offline batch replayed — would otherwise pick whichever row the planner reached
// first, and "what could this patient do" would change between two identical reads.
func (q *Queries) EducationLatestCompetency(ctx context.Context, arg EducationLatestCompetencyParams) ([]EducationLatestCompetencyRow, error) {
	rows, err := q.db.Query(ctx, educationLatestCompetency, arg.PatientID, arg.FacilityID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []EducationLatestCompetencyRow{}
	for rows.Next() {
		var i EducationLatestCompetencyRow
		if err := rows.Scan(
			&i.Code,
			&i.ValueCode,
			&i.EffectiveAt,
			&i.RecordedAt,
			&i.VisitID,
			&i.RecordedBy,
			&i.RecordedRole,
			&i.StationCode,
			&i.Source,
			&i.ChecklistCode,
			&i.Ordinal,
			&i.TextEn,
			&i.TextBn,
			&i.IsCritical,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const educationLatestFlag = `-- name: EducationLatestFlag :one
SELECT o.value_bool, o.effective_at, o.visit_id
  FROM read.observation o
 WHERE o.patient_id = $1::uuid
   AND o.facility_id = $2::uuid
   AND o.code = 'EDU_REEDUCATION_FLAG'
   AND o.status = 'ACTIVE'
 ORDER BY o.effective_at DESC, o.global_seq DESC
 LIMIT 1
`

type EducationLatestFlagParams struct {
	PatientID  uuid.UUID
	FacilityID uuid.UUID
}

type EducationLatestFlagRow struct {
	ValueBool   *bool
	EffectiveAt time.Time
	VisitID     uuid.NullUUID
}

func (q *Queries) EducationLatestFlag(ctx context.Context, arg EducationLatestFlagParams) (EducationLatestFlagRow, error) {
	row := q.db.QueryRow(ctx, educationLatestFlag, arg.PatientID, arg.FacilityID)
	var i EducationLatestFlagRow
	err := row.Scan(&i.ValueBool, &i.EffectiveAt, &i.VisitID)
	return i, err
}

const educationReeducationPolicy = `-- name: EducationReeducationPolicy :one
SELECT unable_raises_flag, corrected_today_threshold
  FROM core.education_reeducation_policy
 WHERE code = 'DEFAULT'
`

type EducationReeducationPolicyRow struct {
	UnableRaisesFlag        bool
	CorrectedTodayThreshold int32
}

func (q *Queries) EducationReeducationPolicy(ctx context.Context) (EducationReeducationPolicyRow, error) {
	row := q.db.QueryRow(ctx, educationReeducationPolicy)
	var i EducationReeducationPolicyRow
	err := row.Scan(&i.UnableRaisesFlag, &i.CorrectedTodayThreshold)
	return i, err
}

const educationUnclassifiedDevices = `-- name: EducationUnclassifiedDevices :many
SELECT p.id AS product_id, g.name AS generic_name, g.class_code,
       m.name_en AS class_name_en, m.name_bn AS class_name_bn
  FROM core.medication_product p
  JOIN core.generic g ON g.id = p.generic_id
  JOIN core.medication_class m ON m.code = g.class_code
 WHERE p.id = ANY ($1::uuid[])
   AND p.facility_id = $2::uuid
   -- Some molecule in this class reaches a checklist, so the station knows this kind of thing.
   AND EXISTS (
     SELECT 1 FROM core.education_device_rule r
       JOIN core.generic sibling ON sibling.id = r.match_generic_id
      WHERE r.retired_at IS NULL AND sibling.class_code = g.class_code)
   -- And this one reaches none.
   AND NOT EXISTS (
     SELECT 1 FROM core.education_device_rule r
      WHERE r.retired_at IS NULL
        AND (r.match_generic_id IS NULL OR r.match_generic_id = p.generic_id)
        AND (r.match_class_code IS NULL OR r.match_class_code = g.class_code)
        AND (r.match_dispense_unit IS NULL OR r.match_dispense_unit = p.dispense_unit)
        AND (r.match_form_code IS NULL OR r.match_form_code = p.form_code))
`

type EducationUnclassifiedDevicesParams struct {
	ProductIds []uuid.UUID
	FacilityID uuid.UUID
}

type EducationUnclassifiedDevicesRow struct {
	ProductID   uuid.UUID
	GenericName string
	ClassCode   string
	ClassNameEn string
	ClassNameBn string
}

// Prescribed lines the station should recognise and does not.
//
// # What this is for, and why an empty checklist list is not enough
//
// A patient on tablets alone brings up no checklist, and that is the right answer. A patient on a
// GLP-1 this formulary gained last year also brings up no checklist — and on the officer's screen
// those two look identical. The second one is a patient who is about to walk out having been
// taught nothing about the pen in their bag.
//
// So the two are separated here. A product is *unclassified* when its class is one this station
// has rules for — some molecule in it selects a checklist — and the product itself matches none
// of them. Metformin's class has no rules at all and is correctly silent; a new GLP-1's class
// does, and is not.
//
// This is the runtime half of spec §6.4's decision not to give the GLP-1 rules a class-level
// fallback. Falling back to the weekly checklist would assert a dosing rhythm nobody confirmed;
// falling back to nothing is safer only if somebody is told, and this is who tells them.
func (q *Queries) EducationUnclassifiedDevices(ctx context.Context, arg EducationUnclassifiedDevicesParams) ([]EducationUnclassifiedDevicesRow, error) {
	rows, err := q.db.Query(ctx, educationUnclassifiedDevices, arg.ProductIds, arg.FacilityID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []EducationUnclassifiedDevicesRow{}
	for rows.Next() {
		var i EducationUnclassifiedDevicesRow
		if err := rows.Scan(
			&i.ProductID,
			&i.GenericName,
			&i.ClassCode,
			&i.ClassNameEn,
			&i.ClassNameBn,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const improvementAnswerForVisit = `-- name: ImprovementAnswerForVisit :many
SELECT o.code, o.value_num, o.value_code, o.effective_at, o.recorded_at,
       o.recorded_by, o.recorded_role, o.station_code, o.source
  FROM read.observation o
 WHERE o.patient_id = $1::uuid
   AND o.facility_id = $2::uuid
   AND o.visit_id = $3::uuid
   AND o.status = 'ACTIVE'
   AND o.code IN ('IMPROVEMENT_SCORE', 'IMPROVEMENT_SCORE_NA')
`

type ImprovementAnswerForVisitParams struct {
	PatientID  uuid.UUID
	FacilityID uuid.UUID
	VisitID    uuid.UUID
}

type ImprovementAnswerForVisitRow struct {
	Code         string
	ValueNum     pgtype.Numeric
	ValueCode    string
	EffectiveAt  time.Time
	RecordedAt   time.Time
	RecordedBy   uuid.UUID
	RecordedRole string
	StationCode  string
	Source       string
}

// The score row and the not-applicable row for one visit.
//
// Both codes in one query, and the caller distinguishes them, because the whole point is that
// they are two different rows and "neither is present" is a third answer. A query that returned
// only the score would make a not-applicable visit indistinguishable from an unasked one.
func (q *Queries) ImprovementAnswerForVisit(ctx context.Context, arg ImprovementAnswerForVisitParams) ([]ImprovementAnswerForVisitRow, error) {
	rows, err := q.db.Query(ctx, improvementAnswerForVisit, arg.PatientID, arg.FacilityID, arg.VisitID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ImprovementAnswerForVisitRow{}
	for rows.Next() {
		var i ImprovementAnswerForVisitRow
		if err := rows.Scan(
			&i.Code,
			&i.ValueNum,
			&i.ValueCode,
			&i.EffectiveAt,
			&i.RecordedAt,
			&i.RecordedBy,
			&i.RecordedRole,
			&i.StationCode,
			&i.Source,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const medicationMissReasons = `-- name: MedicationMissReasons :many
SELECT code, display_en, display_bn, ordering
  FROM core.medication_miss_reason
 WHERE retired_at IS NULL
 ORDER BY ordering
`

type MedicationMissReasonsRow struct {
	Code      string
	DisplayEn string
	DisplayBn string
	Ordering  int32
}

func (q *Queries) MedicationMissReasons(ctx context.Context) ([]MedicationMissReasonsRow, error) {
	rows, err := q.db.Query(ctx, medicationMissReasons)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []MedicationMissReasonsRow{}
	for rows.Next() {
		var i MedicationMissReasonsRow
		if err := rows.Scan(
			&i.Code,
			&i.DisplayEn,
			&i.DisplayBn,
			&i.Ordering,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const proNotApplicableReasons = `-- name: ProNotApplicableReasons :many
SELECT code, display_en, display_bn, ordering
  FROM core.pro_not_applicable_reason
 WHERE retired_at IS NULL
 ORDER BY ordering
`

type ProNotApplicableReasonsRow struct {
	Code      string
	DisplayEn string
	DisplayBn string
	Ordering  int32
}

func (q *Queries) ProNotApplicableReasons(ctx context.Context) ([]ProNotApplicableReasonsRow, error) {
	rows, err := q.db.Query(ctx, proNotApplicableReasons)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ProNotApplicableReasonsRow{}
	for rows.Next() {
		var i ProNotApplicableReasonsRow
		if err := rows.Scan(
			&i.Code,
			&i.DisplayEn,
			&i.DisplayBn,
			&i.Ordering,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const proScale = `-- name: ProScale :one
SELECT code, question_en, question_bn, min_value, max_value, neutral_value
  FROM core.pro_scale
 WHERE observation_code = $1::text AND retired_at IS NULL
`

type ProScaleRow struct {
	Code         string
	QuestionEn   string
	QuestionBn   string
	MinValue     int32
	MaxValue     int32
	NeutralValue int32
}

func (q *Queries) ProScale(ctx context.Context, observationCode string) (ProScaleRow, error) {
	row := q.db.QueryRow(ctx, proScale, observationCode)
	var i ProScaleRow
	err := row.Scan(
		&i.Code,
		&i.QuestionEn,
		&i.QuestionBn,
		&i.MinValue,
		&i.MaxValue,
		&i.NeutralValue,
	)
	return i, err
}

const proScaleAnchors = `-- name: ProScaleAnchors :many
SELECT from_value, to_value, label_en, label_bn, face_rank, ordering
  FROM core.pro_scale_anchor
 WHERE scale_code = $1::text
 ORDER BY ordering
`

type ProScaleAnchorsRow struct {
	FromValue int32
	ToValue   int32
	LabelEn   string
	LabelBn   string
	FaceRank  int32
	Ordering  int32
}

func (q *Queries) ProScaleAnchors(ctx context.Context, scaleCode string) ([]ProScaleAnchorsRow, error) {
	rows, err := q.db.Query(ctx, proScaleAnchors, scaleCode)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ProScaleAnchorsRow{}
	for rows.Next() {
		var i ProScaleAnchorsRow
		if err := rows.Scan(
			&i.FromValue,
			&i.ToValue,
			&i.LabelEn,
			&i.LabelBn,
			&i.FaceRank,
			&i.Ordering,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
