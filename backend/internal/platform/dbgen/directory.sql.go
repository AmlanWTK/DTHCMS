// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.27.0
// source: directory.sql

package dbgen

import (
	"context"

	"github.com/google/uuid"
)

const directoryDevices = `-- name: DirectoryDevices :many
SELECT id, name, kind, status
  FROM core.device
 WHERE facility_id = $1
 ORDER BY name
`

type DirectoryDevicesRow struct {
	ID     uuid.UUID
	Name   string
	Kind   string
	Status string
}

// The tablets and phones, so "which device typed this" is a name rather than a uuid. Retired
// devices stay listed, for the same reason retired staff do.
func (q *Queries) DirectoryDevices(ctx context.Context, facilityID uuid.UUID) ([]DirectoryDevicesRow, error) {
	rows, err := q.db.Query(ctx, directoryDevices, facilityID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []DirectoryDevicesRow{}
	for rows.Next() {
		var i DirectoryDevicesRow
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Kind,
			&i.Status,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const directoryStaff = `-- name: DirectoryStaff :many

SELECT id, employee_code, name_en, name_bn, status
  FROM core.app_user
 WHERE facility_id = $1
 ORDER BY employee_code
`

type DirectoryStaffRow struct {
	ID           uuid.UUID
	EmployeeCode string
	NameEn       string
	NameBn       string
	Status       string
}

// The attribution directory (CP61).
// Everybody who has ever recorded anything, deactivated staff included.
//
// **Deactivated people are in the list on purpose.** Attribution on a value taken last March
// names whoever took it, and half of what a reviewer asks about is somebody who has since left.
// A directory that listed only current staff would render a blank for exactly the person the
// question is about.
//
// Nothing sensitive is here: a name, a staff code and whether they are still with the clinic. No
// contact details, no credentials, no role grants — the role a value carries is the role its
// author was wearing at the time, which is on the value rather than on the person.
func (q *Queries) DirectoryStaff(ctx context.Context, facilityID uuid.UUID) ([]DirectoryStaffRow, error) {
	rows, err := q.db.Query(ctx, directoryStaff, facilityID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []DirectoryStaffRow{}
	for rows.Next() {
		var i DirectoryStaffRow
		if err := rows.Scan(
			&i.ID,
			&i.EmployeeCode,
			&i.NameEn,
			&i.NameBn,
			&i.Status,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const directoryStations = `-- name: DirectoryStations :many
SELECT code, name_en, name_bn, sequence_hint
  FROM core.station
 WHERE facility_id = $1
 ORDER BY sequence_hint
`

type DirectoryStationsRow struct {
	Code         string
	NameEn       string
	NameBn       string
	SequenceHint int32
}

func (q *Queries) DirectoryStations(ctx context.Context, facilityID uuid.UUID) ([]DirectoryStationsRow, error) {
	rows, err := q.db.Query(ctx, directoryStations, facilityID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []DirectoryStationsRow{}
	for rows.Next() {
		var i DirectoryStationsRow
		if err := rows.Scan(
			&i.Code,
			&i.NameEn,
			&i.NameBn,
			&i.SequenceHint,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
