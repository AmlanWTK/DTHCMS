import { PlaceholderPage } from '@/components/PlaceholderPage';

export default function Page() {
  return (
    <PlaceholderPage
      titleKey="page.followUp.title"
      descriptionKey="page.followUp.description"
      areaKey="nav.followUp"
      checkpoint="CP113"
    />
  );
}
