# Next.js
.next/
next-env.d.ts

# Playwright
playwright-report/
test-results/

# Next.js 16 writes these on every `next dev` (next.config.ts `agentRules`). They are a
# generated restatement of the framework's own conventions, not this project's, and a file
# regenerated by a dev server is one that shows up in every diff and is never read.
AGENTS.md
CLAUDE.md
