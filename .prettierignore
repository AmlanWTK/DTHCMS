node_modules/
dist/
build/
.next/
coverage/
# Storybook's static build. Gitignored inside the package, but Prettier only reads the
# root .gitignore, so a `format:check` after a Storybook build would fail on generated
# vendor bundles without this line.
storybook-static/
pnpm-lock.yaml
docs/implementation-plan.md
docs/blueprint-v2.0.md
*.docx
