node_modules/
dist/
build/
.next/
coverage/
# Generated output that is gitignored inside its package. Prettier reads only the root
# .gitignore, so without these lines a `format:check` run after a Storybook build, an
# e2e run, a Metro bundle check, or `next dev` fails on files nobody wrote.
storybook-static/
.expo-export/
.expo/
next-env.d.ts
test-results/
playwright-report/
pnpm-lock.yaml
docs/implementation-plan.md
docs/blueprint-v2.0.md
*.docx
# Generated by `pnpm run spec:docs`. Gitignored; see the note there.
api/docs.html
# The completed questionnaire, retained as the source record for the synthetic data
# profile. A Word document, not something Prettier has an opinion about.
docs/caseload-questionnaire-completed.docx
# Generated from the RBAC engine (CP19); a test compares it byte for byte with the code.
docs/access-matrix.md
# Golden files written by `go test -update`; byte-for-byte is the point.
backend/internal/rbac/testdata/
# The progress table's cells are paragraphs, not phrases. Prettier pads every column to the
# widest cell, which on this file means 268 KB becomes 611 KB of trailing spaces and every future
# diff touches every row. The table is written and read as prose; the formatter's opinion about
# column alignment costs more than it is worth here.
docs/progress.md

# See .gitignore: delivery scaffolding rather than project files, written by whatever produced
# the bundle. `pnpm run verify` should be about this repository, not about a courier's notes.
_sync/
